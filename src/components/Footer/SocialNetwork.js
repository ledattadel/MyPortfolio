import React from 'react'

const SocialNetwork = (props) => {
    return (
        <a href={props.link}>
            <i className={props.class}>

            </i>
        </a>
    )
}

export default SocialNetwork
